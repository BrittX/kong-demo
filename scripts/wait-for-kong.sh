#!/bin/sh
# wait-for-kong.sh

# Initially check to see if the API Gateway is up

# When it's up, check if service has been registered
  # Service not registered, register it with kong and then register routes
